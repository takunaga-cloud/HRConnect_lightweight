# HRConnect 実装改善ポイント（AIエージェント向け）

本ドキュメントは、HRConnectシステムにおける具体的な実装改善ポイントを分類して記載したものです。AIエージェントが直ちに取り組める改善アクションをまとめています。

## 1. ロジックの一貫性と設定性

### 1.1 サービス層におけるビジネスルールのハードコード
*   **課題**: `side_effect.py` 内において、半日休暇の時間（09:00-12:00、13:00-18:00）がハードコードされています。
*   **改善案**: これらの値を `WorkRule.config` またはグローバルな `SystemDefinition` に移行し、異なる会社のポリシーに対応できるようにします。
*   **アクション**: `SideEffectService.apply_paid_leave_side_effects` をリファクタリングし、データベースからこれらの時間を取得するようにします。

### 1.2 複数日にまたがる残業計算ロジック
*   **課題**: `calculator.py` は深夜残業を単純に処理しています（「当日夜〜翌朝のみ」）。24時間を超えるシフトや複雑な夜勤シフトでは正しく動作しない可能性があります。
*   **改善案**: 複数の日付をまたぐ遷移を処理できる、堅牢な時間区間の重複計算ロジックを実装します。
*   **アクション**: `calculate_overtime` を拡張し、日別の境界リストを受け取れるようにします。

## 2. コードの可読性と保守性（読みやすいコード）

### 2.1 例外処理の統一
*   **課題**: 現在、サービス層で `HTTPException`（FastAPI固有）と `BusinessRuleError` などのカスタム例外が混在してスローされています。
*   **改善案**: サービス層はドメイン固有の例外のみをスローするようにします。APIレイヤーでこれらをキャッチし、`HTTPException` にマッピングします。
*   **アクション**: `app/api/deps.py` または専用のミドルウェアに、標準の例外マッパーを作成します。

### 2.2 マジックストリングと定数
*   **課題**: `"PaidLeave"` や `"HalfDayMorning"` などの文字列リテラルや、`"有給休暇申請"` などの日本語文字列がコードベース（`workflow.py`、`side_effect.py`）に散在しています。
*   **改善案**: タイポを防ぎ、検索性を向上させるために、IntEnum または文字列定数を使用します。
*   **アクション**: すべての申請タイプとステータス文字列を `app/core/constants.py` に集約します。

### 2.3 型安全性（Pydantic / 型ヒント）
*   **課題**: 一部の関数で、設定や複雑なオブジェクトに対して `Dict[str, Any]` が使用されています。
*   **改善案**: すべての設定スキーマ（例：`WorkRuleConfig`）に対して構造化された Pydantic モデルを定義し、型ヒントに使用します。
*   **アクション**: `app/schemas/config.py` を作成し、サービスメソッドがこれらのモデルを使用するようにリファクタリングします。

## 3. パフォーマンスとリソース管理

### 3.1 N+1 クエリの防止
*   **課題**: `alert.py` において、ループ内で各ユーザーの `WorkRule` を取得する際、N+1 クエリの問題が発生する可能性があります（一部対応済み）。
*   **改善案**: 最初のユーザー取得クエリで `joinedload` または `selectinload` を使用し、必要なリレーションシップを一度のDBアクセスでまとめて取得します。
*   **アクション**: すべてのリスト取得API（例：`/users`、`/attendances`）を監査し、リレーションシップの事前取得（Eager Loading）が漏れている箇所を修正します。

## 4. 将来のAIエージェント向けドキュメント

### 4.1 主要ロジックにおけるドキュメント文字列（Docstring）の不足
*   **課題**: 多くのファイルに Docstring が記述されていますが、複雑な分岐ロジック（例：`SideEffectService`）において、コメントによる段階的な説明が不足しています。
*   **改善案**: 自明ではないビジネスルールに対して「根拠（Reasoning）」を示すコメントを追加します。
*   **アクション**: 「読みやすいコード」第5章に従い、`side_effect.py` と `calculator.py` に解説コメントを追加します。

## 5. コーディング規約と標準

### 5.1 日付形式の標準化（YYYY/MM/DD）
*   **課題**: ブラウザのロケールの違いにより、日付フィールドが意図せず切り替わる（例：`MM/DD/YYYY` になる）可能性があります。
*   **改善案**: すべてのユーザーインターフェースと日付選択要素において、日付形式を **`YYYY/MM/DD`** に標準化します。`<input type="date">` の直接使用は厳禁とし、統一された `DateInput` コンポーネントを使用する必要があります。
*   **アクション**: フロントエンドの入力が統一されたヘルパーコンポーネントを使用して日付を解析・表示することを確認し、APIリクエストではシリアライズの直前に値を `YYYY-MM-DD` に変換するようにします。
